/**
 * The sidebar helper — what turns a VitePress sidebar into absolute links, so
 * the indexer and the scope picker agree on where a page lives.
 */
export declare function absoluteSidebar<T>(sidebar: T, base?: string): T
